<Window x:Class="Simanis62.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:converters="clr-namespace:Simanis62.Converters"
        xmlns:views="clr-namespace:Simanis62.Views"
        Title="SIMANIS62 - Sistem Manajemen Aset Sekolah"
        Height="768" Width="1280"
        MinHeight="600" MinWidth="1024"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVis"/>
    </Window.Resources>

    <materialDesign:DialogHost Identifier="RootDialog">
        <materialDesign:DrawerHost IsLeftDrawerOpen="{Binding IsSidebarExpanded}">
            <Grid>
                <!-- Login View (Full Screen) -->
                <views:LoginView x:Name="LoginView"
                                 Visibility="{Binding IsLoggedIn, Converter={StaticResource InverseBoolToVis}}"/>

                <!-- Main Shell (After Login) -->
                <Grid Visibility="{Binding IsLoggedIn, Converter={StaticResource BoolToVis}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Sidebar -->
                    <Border Grid.Column="0"
                            Background="{StaticResource PrimaryBrush}"
                            Width="{Binding SidebarWidth}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Logo/Header -->
                            <StackPanel Grid.Row="0" Margin="16,24,16,16">
                                <TextBlock Text="SIMANIS62"
                                           FontSize="24"
                                           FontWeight="Bold"
                                           Foreground="White"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="v2.0"
                                           FontSize="12"
                                           Foreground="#B3FFFFFF"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>

                            <!-- Navigation Menu -->
                            <StackPanel Grid.Row="1">
                                <Button Command="{Binding NavigateToCommand}"
                                        CommandParameter="Dashboard"
                                        Style="{StaticResource SidebarButtonStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="ViewDashboard" Width="24" Height="24" VerticalAlignment="Center"/>
                                        <TextBlock Text="Dashboard" Margin="16,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>

                                <Button Command="{Binding NavigateToCommand}"
                                        CommandParameter="AssetList"
                                        Style="{StaticResource SidebarButtonStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Package" Width="24" Height="24" VerticalAlignment="Center"/>
                                        <TextBlock Text="Daftar Aset" Margin="16,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>

                                <Button Command="{Binding NavigateToCommand}"
                                        CommandParameter="KibReport"
                                        Style="{StaticResource SidebarButtonStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="FileDocument" Width="24" Height="24" VerticalAlignment="Center"/>
                                        <TextBlock Text="Laporan KIB" Margin="16,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>

                                <Button Command="{Binding NavigateToCommand}"
                                        CommandParameter="Mutation"
                                        Style="{StaticResource SidebarButtonStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="SwapHorizontal" Width="24" Height="24" VerticalAlignment="Center"/>
                                        <TextBlock Text="Mutasi Aset" Margin="16,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>

                                <Separator Background="#1976D2" Margin="16,8"/>

                                <!-- Admin Only Menu -->
                                <Button Command="{Binding NavigateToCommand}"
                                        CommandParameter="AssetForm"
                                        Style="{StaticResource SidebarButtonStyle}"
                                        Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVis}}">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Plus" Width="24" Height="24" VerticalAlignment="Center"/>
                                        <TextBlock Text="Tambah Aset" Margin="16,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>

                            <!-- User Info & Logout -->
                            <StackPanel Grid.Row="2" Margin="16">
                                <Separator Background="#1976D2" Margin="0,0,0,16"/>

                                <!-- User Info -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                    <materialDesign:PackIcon Kind="Account"
                                                             Width="32" Height="32"
                                                             Foreground="White"
                                                             VerticalAlignment="Center"/>
                                    <StackPanel Margin="12,0,0,0">
                                        <TextBlock Text="{Binding UserName}"
                                                   Foreground="White"
                                                   FontWeight="Medium"/>
                                        <TextBlock Text="{Binding UserRole}"
                                                   Foreground="#B3FFFFFF"
                                                   FontSize="12"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Logout Button -->
                                <Button Command="{Binding LogoutCommand}"
                                        Style="{StaticResource SidebarButtonStyle}"
                                        HorizontalContentAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Logout" Width="20" Height="20" VerticalAlignment="Center"/>
                                        <TextBlock Text="Keluar" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Main Content Area -->
                    <Grid Grid.Column="1" Background="{StaticResource BackgroundBrush}">
                        <!-- Content Frame -->
                        <Frame x:Name="ContentFrame"
                               NavigationUIVisibility="Hidden"
                               Content="{Binding CurrentView}"/>
                    </Grid>
                </Grid>
            </Grid>
        </materialDesign:DrawerHost>
    </materialDesign:DialogHost>
</Window>
